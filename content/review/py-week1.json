{
  "schemaVersion": 1,
  "week": 1,
  "level": "基础",
  "points": [
    {
      "code": "py.vars.binding",
      "title": "变量绑定、动态类型与 isinstance",
      "minutes": 15,
      "module": "变量与类型",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1201",
          "relation": "introduces"
        },
        {
          "taskId": "1101",
          "relation": "exercises"
        }
      ],
      "concept": {
        "prompt": "变量名和对象之间是什么关系？为什么同一个名字可以先存数字再存字符串？",
        "answer": [
          "变量是绑定到对象的名字，赋值只是重新绑定，不改变量本身的“类型”",
          "类型属于对象，print(type(x)) 看的是当前绑定的对象",
          "isinstance 沿继承链判断，比 type(x) == T 更宽容",
          "bool 是 int 的子类，isinstance(True, int) 为 True"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "x = 10\nx = \"十\"\nprint(type(x).__name__)\ny = x\ny += \"!\"\nprint(x, y)\nprint(isinstance(True, int))",
        "expected": [
          "str",
          "十 十!",
          "True"
        ],
        "explain": "名字 x 重新绑定到字符串对象；str 不可变，y += \"!\" 生成新字符串，x 不变；bool 继承自 int。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "count = \"3\"\ntotal = count + 1\nprint(total)",
        "rootCause": "count 绑定的对象是 str，str + int 会抛 TypeError；动态类型不会替你做隐式转换。",
        "fix": "先显式转换：total = int(count) + 1；转换前还要校验输入，避免 int(\"abc\") 再次抛错。"
      },
      "code_task": {
        "prompt": "写 parse_count(value)：字符串形式的非负整数转成 int，int（bool 除外）原样返回，其他或无法转换的情况返回 None，绝不抛异常。",
        "acceptance": [
          "parse_count(\"3\") 返回 3",
          "parse_count(3) 返回 3",
          "parse_count(None) 与 parse_count(\"--1\") 返回 None",
          "parse_count(True) 返回 None（bool 是 int 子类，要显式排除）"
        ],
        "reference": "def parse_count(value):\n    if isinstance(value, str):\n        text = value.strip()\n        if not text.isdigit():\n            return None\n        return int(text)\n    if isinstance(value, int) and not isinstance(value, bool):\n        return value\n    return None"
      },
      "pitfalls": [
        "以为变量自带固定类型",
        "用 type(x) == int 而不是 isinstance(x, int)",
        "忘记 bool 是 int 的子类",
        "期待 \"3\" + 1 自动转型"
      ]
    },
    {
      "code": "py.vars.truthiness",
      "title": "真值判断、比较与短路求值",
      "minutes": 15,
      "module": "变量与类型",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1301",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "哪些值在 if 里是假？and/or 返回的到底是什么？",
        "answer": [
          "假值：False、None、0、0.0、0j、空字符串、空容器",
          "其余对象按 __bool__（或退化的 __len__）判定真假",
          "and/or 返回的是操作数本身，不是 True/False",
          "短路：a and b 在 a 为假时根本不求值 b，a or b 在 a 为真时不求值 b"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "print(bool([]), bool([0]), bool('0'))\nprint(0 or 'x', 1 and 'y')",
        "expected": [
          "False True True",
          "x y"
        ],
        "explain": "空列表假、含 0 的列表真、'0' 是非空字符串所以真；or/and 返回操作数本身，所以打印出 'x' 和 'y'。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "value = 0\nif value:\n    print('有值')",
        "rootCause": "0 是假值，用真值判断代替了 None 判断，合法的 0 会被当成“没有值”。",
        "fix": "改成 if value is not None:；需要区分“缺省”和“零”时永远用 is None 判断。"
      },
      "code_task": {
        "prompt": "写 safe_div(a, b)：b 为 0 或 None 时返回 None，正常时返回浮点结果；再写 has_value(value) 明确区分“是 None”和“假值”。",
        "acceptance": [
          "safe_div(6, 2) 返回 3.0",
          "safe_div(6, 0) 与 safe_div(6, None) 返回 None",
          "has_value(0) 为 True、has_value(None) 为 False",
          "变体：safe_div(-1, 2) == -0.5（负数与浮点同样走正常路径）"
        ],
        "reference": "def safe_div(a, b):\n    if b is None or b == 0:\n        return None\n    return a / b\n\n\ndef has_value(value):\n    return value is not None"
      },
      "pitfalls": [
        "用真值判断代替 is None",
        "把 '0' 当成假值",
        "把 and/or 的返回值当布尔值用",
        "在短路右侧写有副作用的表达式并期待它一定执行"
      ]
    },
    {
      "code": "py.vars.str-bytes",
      "title": "str 与 bytes、编码与解码",
      "minutes": 10,
      "module": "变量与类型",
      "level": "基础",
      "taskRefs": [],
      "concept": {
        "prompt": "str 和 bytes 有什么区别？len 数的是什么？",
        "answer": [
          "str 是 Unicode 码点序列，len 数“字符”",
          "bytes 是 0~255 的字节序列，len 数“字节”",
          "str -> bytes 用 encode，bytes -> str 用 decode，必须用同一编码",
          "UTF-8 下一个汉字 3 字节，GBK 下 2 字节；编码不对称会出现乱码或 UnicodeDecodeError"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "s = \"中\"\nb = s.encode(\"utf-8\")\nprint(len(s), len(b))\nprint(list(b))\nprint(b.decode(\"utf-8\") == s)",
        "expected": [
          "1 3",
          "[228, 184, 173]",
          "True"
        ],
        "explain": "str 的 len 是字符数 1；UTF-8 编码后是 3 个字节；用同一编码 decode 能还原。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "text = \"hello\"\ndata = text.encode(\"utf-8\")\nwith open(\"note.txt\", \"w\") as handle:\n    handle.write(data)",
        "rootCause": "文本模式（\"w\"）的文件对象只接受 str，写入 bytes 会抛 TypeError。",
        "fix": "要么保持文本模式写 text，要么用二进制模式 open(\"note.txt\", \"wb\") 写 data；两者不能混。"
      },
      "code_task": {
        "prompt": "写 to_bytes(text, encoding=\"utf-8\") 与 decode_text(data, encoding=\"utf-8\")：encode/decode 用同一编码，解码遇到坏字节时用 errors=\"replace\" 不抛异常。",
        "acceptance": [
          "decode_text(to_bytes(\"中\")) 等于 \"中\"",
          "encoding=\"gbk\" 时往返同样成立",
          "decode_text(b\"\\xe4\\xb8\") 不抛 UnicodeDecodeError",
          "to_bytes 返回类型是 bytes"
        ],
        "reference": "def to_bytes(text, encoding=\"utf-8\"):\n    return text.encode(encoding)\n\n\ndef decode_text(data, encoding=\"utf-8\"):\n    return data.decode(encoding, errors=\"replace\")"
      },
      "pitfalls": [
        "把 len(str) 当字节数",
        "文本模式和二进制模式混用",
        "encode/decode 用了不同编码",
        "解码不传 errors 导致 bad data 直接崩"
      ]
    },
    {
      "code": "py.vars.operators",
      "title": "运算符、增强赋值与解包赋值",
      "minutes": 15,
      "module": "变量与类型",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1301",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "// 和 % 的关系是什么？星号解包和解包赋值各有什么规则？",
        "answer": [
          "/ 是浮点除法，// 是向下取整的整除，% 与 // 满足 a == (a // b) * b + a % b",
          "divmod(a, b) 一次拿到 (商, 余) 元组",
          "a, *rest = seq 把第一个给 a，其余收集成 list；* 只能出现一次",
          "浮点数不能直接用 == 比较，要用 math.isclose 或容差"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "print(7 // 2, 7 % 2, 2 ** 3)\nprint(divmod(7, 2))\na, *b = [1, 2, 3, 4]\nprint(a, b)\nx = 5\nx += 2\nprint(x)\nprint(0.1 + 0.2 == 0.3)",
        "expected": [
          "3 1 8",
          "(3, 1)",
          "1 [2, 3, 4]",
          "7",
          "False"
        ],
        "explain": "// 向下取整并与 % 配套；divmod 返回元组；星号解包把剩余元素收成 list；浮点运算有二进制误差，0.1+0.2 != 0.3。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "pages = 10\nper_page = 3\nprint(pages / per_page + 1)",
        "rootCause": "想算“需要几页”，但 / 得到的是浮点数 3.33… 再 +1 = 4.33，而不是向上取整的 4；用 / 不会自动进位。",
        "fix": "在整数上向上取整：print(-(-pages // per_page))，或 import math 后 math.ceil(pages / per_page)。"
      },
      "code_task": {
        "prompt": "写 split_bill(total_fen, people)：把总金额（单位分）尽量平均分给 people 个人，返回 (每人分到, 剩余)。",
        "acceptance": [
          "split_bill(100, 3) == (33, 1)",
          "split_bill(100, 2) == (50, 0)",
          "split_bill(0, 5) == (0, 0)",
          "people <= 0 时抛 ValueError"
        ],
        "reference": "def split_bill(total_fen, people):\n    if people <= 0:\n        raise ValueError(\"people 必须为正\")\n    return divmod(total_fen, people)"
      },
      "pitfalls": [
        "用 / 却期待整数结果",
        "负数取模方向搞反",
        "用 == 比较浮点结果",
        "解包时元素个数不匹配导致 ValueError"
      ]
    },
    {
      "code": "py.list.basics",
      "title": "列表与元组：索引、切片、可变性、增删改",
      "minutes": 20,
      "module": "容器",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1202",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "切片和索引有什么不同？元组真的完全不可变吗？",
        "answer": [
          "索引取单个元素，越界抛 IndexError；切片返回新对象，越界只截断不报错",
          "nums[i] = v 原地改；nums.append/pop/insert/remove/del 都是原地操作",
          "tuple 本身不可变，但元组里装的可变对象仍可被修改",
          "a[:] 是浅拷贝，外层新、内层仍共享"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "nums = [1, 2, 3, 4, 5]\nprint(nums[1:4], nums[::2])\nnums[0] = 9\nnums.append(6)\nprint(nums)\nt = (1, [2, 3])\nt[1].append(4)\nprint(t)\nprint(nums[-2:], len(nums))",
        "expected": [
          "[2, 3, 4] [1, 3, 5]",
          "[9, 2, 3, 4, 5, 6]",
          "(1, [2, 3, 4])",
          "[5, 6] 6"
        ],
        "explain": "切片返回新列表（步长 2 取 1/3/5）；索引赋值与 append 原地修改；元组不可变但里面的 list 可变；负索引从尾部数。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def add_tag(tags):\n    tags = tags + [\"new\"]\n    return tags\n\n\ntags = [\"a\"]\nadd_tag(tags)\nprint(tags)",
        "rootCause": "tags = tags + [...] 新建了列表并只把局部名字重绑到新列表，调用方的 tags 完全没被改。",
        "fix": "原地修改用 tags.append(\"new\")；若坚持返回新列表，就让调用方接收返回值 tags = add_tag(tags)。"
      },
      "code_task": {
        "prompt": "写 take_while_positive(nums)：返回从头开始、遇到第一个非正数之前的元素副本；不修改原列表。",
        "acceptance": [
          "[1, 2, 0, 3] 返回 [1, 2]",
          "全是正数时返回全部元素",
          "返回的是新列表（对结果 append 不影响原列表）",
          "空列表返回 []"
        ],
        "reference": "def take_while_positive(nums):\n    result = []\n    for value in nums:\n        if value <= 0:\n            break\n        result.append(value)\n    return result"
      },
      "pitfalls": [
        "把切片当成原地修改",
        "以为元组里的 list 也不能改",
        "pop/remove/del 语义混淆",
        "索引越界或负索引算错位置"
      ]
    },
    {
      "code": "py.list.sort-key",
      "title": "sorted/sort 与 key、稳定性",
      "minutes": 15,
      "module": "容器",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1202",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "list.sort() 和 sorted() 有什么区别？key 应该怎么写？",
        "answer": [
          "list.sort() 原地排序返回 None；sorted() 返回新列表",
          "key 接收一个函数，比较的是它的返回值；多字段用元组",
          "Python 排序稳定：key 相同的元素保持原相对顺序",
          "降序用 reverse=True，或对数值字段取负"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "rows = [(\"b\", 2), (\"a\", 2), (\"c\", 1)]\nrows.sort(key=lambda row: (row[1], row[0]))\nprint(rows)\nwords = [\"pear\", \"fig\", \"apple\"]\nprint(sorted(words, key=len))\nprint(sorted(words, key=len, reverse=True))",
        "expected": [
          "[('c', 1), ('a', 2), ('b', 2)]",
          "['fig', 'pear', 'apple']",
          "['apple', 'pear', 'fig']"
        ],
        "explain": "key 返回 (分数, 名字) 元组，先按分数升序、同分按名字升序；按长度排序时 fig(3) < pear(4) < apple(5)。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "scores = {\"a\": 3, \"b\": 1, \"c\": 2}\ntop = sorted(scores, key=scores.get)\nprint(top[0])",
        "rootCause": "sorted 默认升序，key=scores.get 取到的是最小分数的键，top[0] 拿的是最低分而不是最高分。",
        "fix": "写清意图：sorted(scores, key=lambda name: -scores[name])[0]，或 sorted(scores, key=scores.get, reverse=True)[0]。"
      },
      "code_task": {
        "prompt": "写 rank_items(items)：items 是 (name, score) 列表，按分数从高到低、同分按名字字典序返回名字列表，且不修改入参。",
        "acceptance": [
          "rank_items([(\"b\", 1), (\"a\", 1)]) == [\"a\", \"b\"]",
          "rank_items([(\"a\", 1), (\"b\", 2)]) == [\"b\", \"a\"]",
          "调用后原 items 顺序不变",
          "变体：分数为负数时仍按数值降序（rank_items([(\"a\", -1), (\"b\", -2)]) == [\"a\", \"b\"]）"
        ],
        "reference": "def rank_items(items):\n    return [name for name, _ in sorted(items, key=lambda item: (-item[1], item[0]))]"
      },
      "pitfalls": [
        "把 key=len() 写成调用结果或漏参数",
        "以为 sort 返回新列表（拿到 None）",
        "对 dict.items() 直接排序导致比较到 dict",
        "误以为排序不稳定而不敢依赖同分顺序"
      ]
    },
    {
      "code": "py.dict.basics",
      "title": "dict 键值语义、迭代顺序与哈希约束",
      "minutes": 20,
      "module": "容器",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1202",
          "relation": "introduces"
        },
        {
          "taskId": "1203",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "dict 的键有什么要求？为什么 [] 取不存在的键会报错而 get 不会？",
        "answer": [
          "键必须可哈希（不可变），list/dict/set 不能当键，tuple 元素都可哈希时才行",
          "d[k] 键不存在抛 KeyError；d.get(k, default) 返回默认值",
          "Python 3.7+ 保证按插入顺序迭代 keys/values/items",
          "遍历时增删键会抛 RuntimeError，需要先收集键再改"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "d = {\"a\": 1, \"b\": 2}\nd[\"c\"] = 3\nprint(list(d))\nprint(d.get(\"x\", 0), \"x\" in d)\nd[\"a\"] = d.get(\"a\", 0) + 10\nprint(d)\nprint(list(d.items())[:2])",
        "expected": [
          "['a', 'b', 'c']",
          "0 False",
          "{'a': 11, 'b': 2, 'c': 3}",
          "[('a', 11), ('b', 2)]"
        ],
        "explain": "新键追加在末尾、迭代保持插入顺序；get 提供默认值且不影响成员判断；items() 给出 (键, 值) 元组。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "counts = {}\nfor word in [\"a\", \"b\", \"a\"]:\n    counts[word] += 1",
        "rootCause": "对还不存在的键做 += 会先取值，第一次循环就抛 KeyError。",
        "fix": "先给初值再累加：counts[word] = counts.get(word, 0) + 1；或直接用 collections.Counter。"
      },
      "code_task": {
        "prompt": "写 invert_map(mapping)：把 {键: 值} 反转成 {值: [键, ...]}，同一值的多个键按字典序排列。",
        "acceptance": [
          "invert_map({\"a\": 1, \"b\": 1}) == {1: [\"a\", \"b\"]}",
          "空 dict 返回 {}",
          "结果里的值都是 list",
          "不修改传入的 mapping"
        ],
        "reference": "def invert_map(mapping):\n    result = {}\n    for key, value in mapping.items():\n        result.setdefault(value, []).append(key)\n    for keys in result.values():\n        keys.sort()\n    return result"
      },
      "pitfalls": [
        "用 [] 访问可能不存在的键",
        "以为 dict 无序",
        "拿 list/dict 当键",
        "遍历时增删键"
      ]
    },
    {
      "code": "py.dict.aggregate",
      "title": "分组聚合与词频统计",
      "minutes": 20,
      "module": "容器",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1102",
          "relation": "introduces"
        },
        {
          "taskId": "1204",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "做分组统计时通用的模式是什么？Counter 帮了什么忙？",
        "answer": [
          "模式：遍历记录 -> 计算分组键 -> setdefault/get 初始化累加器 -> 累加",
          "计数用 counts[key] = counts.get(key, 0) + 1，或 Counter(可迭代)",
          "分组用 groups.setdefault(key, []).append(value)",
          "Counter.most_common 按计数降序，计数相同则按首次出现顺序，要确定顺序得自己 sorted"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "from collections import Counter\n\nwords = \"a b a c b a\".split()\nprint(Counter(words).most_common(2))\ngroups = {}\nfor word in words:\n    groups.setdefault(len(word), []).append(word)\nprint(sorted(groups.items()))",
        "expected": [
          "[('a', 3), ('b', 2)]",
          "[(1, ['a', 'b', 'a', 'c', 'b', 'a'])]"
        ],
        "explain": "四个词长度都是 1，分到同一组；most_common(2) 按计数降序取前两名。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "sales = [(\"a\", 10), (\"b\", 5), (\"a\", 3)]\ntotals = {}\nfor name, amount in sales:\n    totals[name] = amount\nprint(totals[\"a\"])",
        "rootCause": "直接赋值把前一条记录覆盖掉了，聚合变成了“只留最后一条”。",
        "fix": "改成累加：totals[name] = totals.get(name, 0) + amount。"
      },
      "code_task": {
        "prompt": "写 top_words(text, n)：按空白切词，返回出现次数最多的 n 个 (word, count)；次数相同按字母升序；n <= 0 返回空列表。",
        "acceptance": [
          "top_words(\"a b a\", 1) == [(\"a\", 2)]",
          "次数相同时按字母升序：top_words(\"b a\", 2) == [(\"a\", 1), (\"b\", 1)]",
          "n 大于词数时返回全部",
          "变体：n <= 0（含负数）返回 []，空文本返回 []"
        ],
        "reference": "from collections import Counter\n\n\ndef top_words(text, n):\n    if n <= 0:\n        return []\n    counts = Counter(text.split())\n    return sorted(counts.items(), key=lambda item: (-item[1], item[0]))[:n]"
      },
      "pitfalls": [
        "忘记给不存在的键初始化",
        "用赋值代替累加",
        "直接依赖 most_common 的同计数顺序",
        "大小写/标点没归一导致词被拆散"
      ]
    },
    {
      "code": "py.set.dedupe",
      "title": "set 运算与去重（含保序去重）",
      "minutes": 20,
      "module": "容器",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1202",
          "relation": "introduces"
        },
        {
          "taskId": "1204",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "set 有什么特性？为什么它不能保序、也不能放 list？",
        "answer": [
          "set 是“可哈希对象的无序集合”，元素唯一，成员测试平均 O(1)",
          "元素必须可哈希，list/dict/set 不能放进去；frozenset 可以",
          "运算：& 交、| 并、- 差、^ 对称差、<= 子集",
          "1、1.0、True 两两相等且哈希相同，在 set 里算同一个元素"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "nums = [3, 1, 3, 2, 1]\nprint(sorted(set(nums)))\na, b = {1, 2, 3}, {2, 3, 4}\nprint(sorted(a & b), sorted(a | b), sorted(a - b))\nprint(len({1, 1.0, True}))",
        "expected": [
          "[1, 2, 3]",
          "[2, 3] [1, 2, 3, 4] [1]",
          "1"
        ],
        "explain": "set 去重且无序，打印前要 sorted；& | - 是交并差；1、1.0、True 相等且哈希相同，只保留一个。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "seen = set()\n\n\ndef dedupe(items):\n    result = []\n    for item in items:\n        if item not in seen:\n            result.append(item)\n        seen.add(item)\n    return result\n\n\nprint(dedupe([1, 2]))\nprint(dedupe([1, 3]))",
        "rootCause": "seen 定义在函数外，多次调用之间共享：第二次调用时 1 已经在 seen 里，于是被静默丢掉，返回 [3] 而不是 [1, 3]。",
        "fix": "把 seen = set() 放进函数内部，让它成为每次调用独立的局部状态。"
      },
      "code_task": {
        "prompt": "写 ordered_unique(items)：按首次出现顺序去重，返回新 list；元素必须可哈希。",
        "acceptance": [
          "ordered_unique([3, 1, 3, 2]) == [3, 1, 2]",
          "空列表返回 []（返回的是 list 不是 set）",
          "返回新列表，原列表不变",
          "变体：全为重复值时长度为 1（ordered_unique([7, 7, 7]) == [7]）"
        ],
        "reference": "def ordered_unique(items):\n    seen = set()\n    result = []\n    for item in items:\n        if item in seen:\n            continue\n        seen.add(item)\n        result.append(item)\n    return result"
      },
      "pitfalls": [
        "依赖 set 的迭代顺序",
        "把 seen 放到函数外跨调用共享",
        "把不可哈希对象塞进 set",
        "混淆 set 与 frozenset 的可变性"
      ]
    },
    {
      "code": "py.container.selection",
      "title": "容器选型与复杂度（list/tuple/dict/set/deque）",
      "minutes": 30,
      "module": "容器",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1202",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "什么场景该选 list、tuple、dict、set、deque？",
        "answer": [
          "list：有序、可重复、按下标访问快，头部插入/删除 O(n)",
          "tuple：固定不变的小记录，可哈希、可当 dict 键",
          "dict：键值映射，按 key 查找/插入平均 O(1)，保持插入顺序",
          "set：只要“是否存在/去重/集合运算”，成员测试平均 O(1)",
          "deque：两端 O(1) 进出的队列，需要头部弹出时替代 list"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "from collections import deque\n\nitems = deque([1, 2, 3])\nitems.appendleft(0)\nprint(list(items))\nitems.pop()\nprint(list(items))\nmapping = {\"a\": 1}\nprint(mapping[\"a\"], mapping.get(\"b\", \"缺失\"))",
        "expected": [
          "[0, 1, 2, 3]",
          "[0, 1, 2]",
          "1 缺失"
        ],
        "explain": "deque 左端 appendleft 与右端 pop 都是 O(1)；dict 按 key 取值 O(1)，get 对缺席返回默认值。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def pop_first(items):\n    return items.pop(0)",
        "rootCause": "list.pop(0) 之后所有元素都要前移，复杂度 O(n)；高频从头部取数据时选错了容器。",
        "fix": "改用 collections.deque，用 popleft() 实现 O(1) 头部弹出；只是偶尔取头部则无所谓。"
      },
      "code_task": {
        "prompt": "写 Recent(capacity)：add(item) 只保留最近 capacity 条记录，latest() 返回最新在前的列表；capacity <= 0 抛 ValueError。",
        "acceptance": [
          "Recent(2) 连续 add 1、2、3 后 latest() == [3, 2]",
          "容量没满时 latest() 返回已加入的全部（最新在前）",
          "Recent(0) 抛 ValueError",
          "变体：连续 add 大量数据后长度恒等于 capacity（用 deque(maxlen=...) 保证 O(1) 与定长）"
        ],
        "reference": "from collections import deque\n\n\nclass Recent:\n    def __init__(self, capacity):\n        if capacity <= 0:\n            raise ValueError(\"capacity 必须为正\")\n        self.items = deque(maxlen=capacity)\n\n    def add(self, item):\n        self.items.append(item)\n\n    def latest(self):\n        return list(reversed(self.items))"
      },
      "pitfalls": [
        "需要头部插入却用 list.insert(0, ...)",
        "用 list 做大量成员测试",
        "需要按键查找却存成 list of tuples",
        "对 deque 用切片或随机下标"
      ]
    },
    {
      "code": "py.nested.structures",
      "title": "嵌套结构转换与浅拷贝陷阱",
      "minutes": 25,
      "module": "容器",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1102",
          "relation": "introduces"
        },
        {
          "taskId": "1201",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "嵌套列表/字典做转换和复制时最容易踩什么坑？",
        "answer": [
          "[[0] * cols] * rows 复制的是同一行的引用，改一行全变",
          "copy.copy / dict() / a[:] 都是浅拷贝：外层新，嵌套对象仍共享",
          "需要完全独立用 copy.deepcopy，或手写递归构造",
          "嵌套结构转换优先用推导式，语义清晰且不容易共享引用"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "import copy\n\nmatrix = [[1, 2], [3, 4]]\nshallow = copy.copy(matrix)\nshallow[0][0] = 99\nprint(matrix)\ndeep = copy.deepcopy(matrix)\ndeep[1][1] = 88\nprint(matrix[1][1], deep[1][1])\nrows = [{\"n\": i} for i in range(2)]\nprint([row[\"n\"] for row in rows])",
        "expected": [
          "[[99, 2], [3, 4]]",
          "4 88",
          "[0, 1]"
        ],
        "explain": "浅拷贝只复制外层 list，内层 list 仍共享，所以改 shallow 影响了 matrix；deepcopy 递归复制，改 deep 不影响原对象。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def make_grid(rows, cols):\n    return [[0] * cols] * rows\n\n\ngrid = make_grid(2, 2)\ngrid[0][0] = 1\nprint(grid)",
        "rootCause": "[[0] * cols] * rows 让每一行都指向同一个内层 list，改一行等于改所有行。",
        "fix": "用推导式逐行新建：[[0] * cols for _ in range(rows)]。"
      },
      "code_task": {
        "prompt": "写 flatten(matrix)：把两层嵌套结构摊平成一维新列表；元素本身是 list 就展开，不是就原样放入。",
        "acceptance": [
          "flatten([[1, 2], [3]]) == [1, 2, 3]",
          "flatten([[], [1]]) == [1]",
          "flatten([1, 2]) == [1, 2]（非嵌套元素直接放入）",
          "不修改入参 matrix"
        ],
        "reference": "def flatten(matrix):\n    result = []\n    for row in matrix:\n        if isinstance(row, list):\n            result.extend(row)\n        else:\n            result.append(row)\n    return result"
      },
      "pitfalls": [
        "用 * 复制嵌套列表",
        "把浅拷贝当深拷贝",
        "对含循环引用的结构无脑 deepcopy",
        "原地修改嵌套结构后发现别名一起变了"
      ]
    },
    {
      "code": "py.mutability.alias",
      "title": "可变/不可变与别名",
      "minutes": 20,
      "module": "对象语义",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1201",
          "relation": "introduces"
        },
        {
          "taskId": "1203",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "为什么 b = a 之后改 b 会影响 a？哪些对象会这样？",
        "answer": [
          "赋值不复制对象，只让两个名字指向同一个对象（别名）",
          "list/dict/set 等可变对象通过别名互相影响；int/str/tuple 不可变，重新赋值只是重绑名字",
          "a is b 判断是否同一对象，a == b 判断值是否相等",
          "想互不影响必须显式复制（copy / 切片 / 推导式新建）"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "a = [1, 2]\nb = a\nb.append(3)\nprint(a, a is b)\nc = [1, 2, 3]\nprint(c == a, c is a)\ns = \"ab\"\nt = s\nt += \"c\"\nprint(s, t)",
        "expected": [
          "[1, 2, 3] True",
          "True False",
          "ab abc"
        ],
        "explain": "b = a 是别名，append 原地改的是同一个列表；c 是新列表，值相等但身份不同；str 不可变，t += 生成新字符串，s 不变。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def clear(items):\n    items = []\n    return items\n\n\ndata = [1, 2]\nclear(data)\nprint(data)",
        "rootCause": "items = [] 只是把函数内的局部名字重绑到新列表，调用方的 data 一个元素都没少。",
        "fix": "要清空调用方的列表就原地操作：items.clear()（或 del items[:]）；不要用重新赋值假装清空。"
      },
      "code_task": {
        "prompt": "写 append_all(target, values)：把 values 的元素原地追加进 target 并返回 target；即使 values 就是 target 本身也必须正常结束。",
        "acceptance": [
          "返回的就是同一个 target 对象（result is target 为真）",
          "target 被原地修改，调用方能看到新元素",
          "append_all(x, x) 不会无限循环且元素翻倍",
          "values 为空时 target 不变"
        ],
        "reference": "def append_all(target, values):\n    for value in list(values):\n        target.append(value)\n    return target"
      },
      "pitfalls": [
        "把别名当成副本",
        "以为函数参数默认是拷贝",
        "用 items = [] 清空别人的列表",
        "对不可变对象也担心别名问题"
      ]
    },
    {
      "code": "py.mutability.copy-deep",
      "title": "浅拷贝与深拷贝",
      "minutes": 20,
      "module": "对象语义",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1201",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "浅拷贝复制了什么？什么时候必须用 deepcopy？",
        "answer": [
          "浅拷贝（copy.copy / dict() / list() / a[:]）只新建外层容器，嵌套对象仍共享",
          "深拷贝（copy.deepcopy）递归复制所有层，副本与原对象完全独立",
          "不可变对象 deepcopy 可能直接返回原对象，这是安全的优化",
          "deepcopy 处理循环引用靠 memo，但代价高；能推导式重建就别 deepcopy"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "import copy\n\ninner = [1, 2]\ndata = {\"inner\": inner}\nshallow = copy.copy(data)\nshallow[\"inner\"].append(3)\nprint(data[\"inner\"])\ndeep = copy.deepcopy(data)\ndeep[\"inner\"].append(4)\nprint(data[\"inner\"], deep[\"inner\"])\nshallow[\"new\"] = 1\nprint(\"new\" in data, \"new\" in shallow)",
        "expected": [
          "[1, 2, 3]",
          "[1, 2, 3] [1, 2, 3, 4]",
          "False True"
        ],
        "explain": "浅拷贝后 shallow[\"inner\"] 与原对象是同一个 list；deepcopy 之后两边互不影响；新增顶层键只影响副本自己。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "config = {\"retries\": 3, \"tags\": [\"a\"]}\nclone = dict(config)\nclone[\"tags\"].append(\"b\")\nprint(config[\"tags\"])",
        "rootCause": "dict(config) 是浅拷贝，clone[\"tags\"] 与 config[\"tags\"] 是同一个 list，改副本等于改原配置。",
        "fix": "用 copy.deepcopy(config)，或对可变字段单独复制：clone[\"tags\"] = list(config[\"tags\"])。"
      },
      "code_task": {
        "prompt": "写 clone_config(value)：手写递归复制嵌套 dict/list，返回值与原对象不共享任何可变部分；不得直接调用 copy.deepcopy。",
        "acceptance": [
          "修改副本里的嵌套 list 不影响原对象",
          "修改副本里嵌套 dict 的值不影响原对象",
          "标量、str、tuple 等不可变值可原样返回",
          "空 dict / 空 list 也能正常复制"
        ],
        "reference": "def clone_config(value):\n    if isinstance(value, dict):\n        return {key: clone_config(item) for key, item in value.items()}\n    if isinstance(value, list):\n        return [clone_config(item) for item in value]\n    return value"
      },
      "pitfalls": [
        "dict()/list() 当深拷贝用",
        "只复制一层就以为独立",
        "对循环引用无脑 deepcopy",
        "copy 之后改顶层键还担心影响原对象"
      ]
    },
    {
      "code": "py.mutability.arg-passing",
      "title": "传参是传对象引用",
      "minutes": 15,
      "module": "对象语义",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1101",
          "relation": "exercises"
        },
        {
          "taskId": "1503",
          "relation": "exercises"
        }
      ],
      "concept": {
        "prompt": "Python 是传值还是传引用？函数里改参数会反映到调用方吗？",
        "answer": [
          "传的是对象引用（按对象共享传参），形参是实参对象的别名",
          "对可变对象做原地操作（append/update/clear）会反映到调用方",
          "在函数内重新赋值（items = ...）只重绑局部名，不影响调用方",
          "不可变对象（str/int/tuple）永远不受影响，函数只能靠返回值交出结果"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def add_item(items, item):\n    items.append(item)\n\n\ndef rebind(items):\n    items = items + [\"x\"]\n\n\ndata = [1]\nadd_item(data, 2)\nrebind(data)\nprint(data)\n\n\ndef mutate(text):\n    text = text.upper()\n    return text\n\n\ns = \"hi\"\nmutate(s)\nprint(s)",
        "expected": [
          "[1, 2]",
          "hi"
        ],
        "explain": "add_item 原地 append 影响了调用方；rebind 新建列表并重绑局部名，没有影响；str 不可变，mutate 返回的新字符串没被使用，s 仍是 hi。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def normalize(names):\n    names = [name.strip() for name in names]\n    return names\n\n\nraw = [\" a \", \"b \"]\nnormalize(raw)\nprint(raw)",
        "rootCause": "推导式新建了列表并只重绑局部名，调用方的 raw 仍是带空白的原始数据。",
        "fix": "要么用返回值 raw = normalize(raw)，要么原地替换 names[:] = [...]。"
      },
      "code_task": {
        "prompt": "写 dedupe_in_place(nums)：原地去重并保持首次出现顺序，返回同一个列表对象（让调用方既拿到返回值也看到原地效果）。",
        "acceptance": [
          "返回的对象 is 原列表",
          "保持首次出现顺序：[3, 1, 3, 2] -> [3, 1, 2]",
          "调用方的列表确实被改短了",
          "空列表与全重复列表都正常"
        ],
        "reference": "def dedupe_in_place(nums):\n    seen = set()\n    write = 0\n    for value in nums:\n        if value in seen:\n            continue\n        seen.add(value)\n        nums[write] = value\n        write += 1\n    del nums[write:]\n    return nums"
      },
      "pitfalls": [
        "以为参数默认按值拷贝",
        "在函数里重新赋值期待影响调用方",
        "顺手原地修改了调用方的数据",
        "对不可变对象调用原地方法"
      ]
    },
    {
      "code": "py.mutability.default-arg",
      "title": "可变默认参数与求值时机",
      "minutes": 20,
      "module": "函数",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1103",
          "relation": "introduces"
        },
        {
          "taskId": "1303",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "默认参数什么时候求值？为什么可变默认值会在调用之间累积？",
        "answer": [
          "默认值在函数定义时求值一次，保存在函数对象里",
          "可变默认值（list/dict/set）被所有未显式传参的调用共享",
          "每次调用都要新对象就用 None 哨兵，在函数体内新建",
          "想显式使用共享缓存时，共享是特性；区分不了就别用可变默认值"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def add(item, items=[]):\n    items.append(item)\n    return items\n\n\nprint(add(1))\nprint(add(2))\nprint(add(3, []))\nprint(add(4))",
        "expected": [
          "[1]",
          "[1, 2]",
          "[3]",
          "[1, 2, 4]"
        ],
        "explain": "默认列表在 def 执行时创建一次并被所有未传参的调用共享；第 3 行显式传了新列表所以是独立的 [3]；第 4 行又回到那个共享列表，于是变成 [1, 2, 4]。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def cache(key, store={}):\n    if key not in store:\n        store[key] = []\n    return store[key]\n\n\nprint(cache(\"a\"))\nprint(cache(\"b\"))\nprint(cache(\"a\"))",
        "rootCause": "store 在函数定义时创建一次并被所有调用共享，看起来像缓存，实际是不可控的跨调用状态：测试之间会互相污染，进程内不同业务也会串数据。",
        "fix": "改成 store=None 哨兵：def cache(key, store=None): store = {} if store is None else store；确实需要共享缓存就显式传入同一个 dict，别藏在默认值里。"
      },
      "code_task": {
        "prompt": "用 None 哨兵重写 add(item, items=None)，并写测试证明多次调用互不影响、显式传入时按传入的列表来。",
        "acceptance": [
          "连续调用 add(1)、add(2) 分别返回 [1] 和 [2]",
          "add(1, shared) 会把元素加进 shared 并返回它",
          "函数不会修改自己新建的默认列表以外的任何东西",
          "带一个 unittest 用例覆盖“两次调用结果独立”"
        ],
        "reference": "def add(item, items=None):\n    if items is None:\n        items = []\n    items.append(item)\n    return items"
      },
      "pitfalls": [
        "默认值是可变对象",
        "把默认值当每次新建",
        "调用侧共享同一列表",
        "在默认值里藏缓存导致测试互相污染"
      ]
    },
    {
      "code": "py.identity.eq-vs-is",
      "title": "== 与 is、驻留、None 判断",
      "minutes": 20,
      "module": "对象语义",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1201",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "== 和 is 分别比什么？为什么判断 None 必须用 is？",
        "answer": [
          "== 调用 __eq__ 比值；is 比较内存身份（是否同一个对象）",
          "小整数与部分字符串会被驻留复用，is 的结果不可依赖",
          "None 是单例，判断缺失一律用 is None / is not None",
          "别用 == 和 None 比较：自定义 __eq__ 可能返回反直觉结果"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "a = [1, 2]\nb = a[:]\nprint(a == b, a is b)\nx = None\nprint(x is None, x == None)\ny = []\nprint(bool(y), y is None)",
        "expected": [
          "True False",
          "True True",
          "False False"
        ],
        "explain": "切片产生新列表，值相等但身份不同；None 单例两种写法都为真，但规范用 is；空列表是假值，但它不是 None。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def is_same_name(a, b):\n    return a is b\n\n\nprint(is_same_name(\"张\" * 2, \"张张\"))",
        "rootCause": "用 is 比较运行时拼出来的字符串，身份是否相同取决于驻留策略，不是语言保证，结果会随实现和上下文变化。",
        "fix": "比较内容用 ==：return a == b；is 只保留给 None 等单例或“确实要同一对象”的场景。"
      },
      "code_task": {
        "prompt": "写 same_content(a, b) 比较内容、is_missing(value) 判断缺失，并写 find_or_none(items, target) 找不到时返回 None。",
        "acceptance": [
          "same_content([1], [1]) 为 True，same_content([1], [2]) 为 False",
          "is_missing(None) 为 True，is_missing(0) 为 False",
          "find_or_none([1, 2], 2) 返回 2",
          "find_or_none([1, 2], 3) 返回 None，且调用方应能用 is None 判断"
        ],
        "reference": "def same_content(a, b):\n    return a == b\n\n\ndef is_missing(value):\n    return value is None\n\n\ndef find_or_none(items, target):\n    for item in items:\n        if item == target:\n            return item\n    return None"
      },
      "pitfalls": [
        "用 is 比较字符串或数字",
        "用 == 判断 None",
        "把 == 当成身份比较",
        "依赖小整数/字符串驻留"
      ]
    },
    {
      "code": "py.identity.eq-hash",
      "title": "__eq__ 与 __hash__ 的关系",
      "minutes": 15,
      "module": "对象语义",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1203",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "为什么重写 __eq__ 后对象就不能放进 set/dict 了？哈希契约是什么？",
        "answer": [
          "相等对象必须有相同哈希：a == b 则 hash(a) == hash(b)",
          "类里定义 __eq__ 时 __hash__ 被自动置为 None，实例变成不可哈希",
          "哈希相同不代表相等，冲突靠 __eq__ 再判一次",
          "不可变值对象推荐 dataclass(frozen=True) 或 namedtuple，自动生成一致的 __eq__/__hash__"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "class Point:\n    def __init__(self, x, y):\n        self.x, self.y = x, y\n\n    def __eq__(self, other):\n        return (self.x, self.y) == (other.x, other.y)\n\n    def __hash__(self):\n        return hash((self.x, self.y))\n\n\np1, p2 = Point(1, 2), Point(1, 2)\nprint(p1 == p2, hash(p1) == hash(p2))\nprint(len({p1, p2}))\nprint({p1: \"a\"}[p2])",
        "expected": [
          "True True",
          "1",
          "a"
        ],
        "explain": "同时定义了 __eq__ 和 __hash__，相等对象哈希相同，set 去重后只剩 1 个，dict 也能用等价的键查到值。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "class User:\n    def __init__(self, uid):\n        self.uid = uid\n\n    def __eq__(self, other):\n        return self.uid == other.uid\n\n\nusers = {User(1): \"amy\"}\nprint(User(1) in users)",
        "rootCause": "定义了 __eq__ 却没有定义 __hash__，Python 把 __hash__ 置为 None，User 实例不可哈希，构造 dict 的那一刻就抛 TypeError: unhashable type。",
        "fix": "补上 __hash__（如 return hash(self.uid)），或直接用 @dataclass(frozen=True) / namedtuple 让两者自动保持一致。"
      },
      "code_task": {
        "prompt": "用 dataclass(frozen=True) 定义 Point(x, y)，写 unique_points(points) 按值去重并保持首次出现顺序。",
        "acceptance": [
          "Point(1, 2) == Point(1, 2) 且 hash 相等",
          "len({Point(1, 2), Point(1, 2)}) == 1",
          "unique_points([Point(1, 2), Point(1, 2), Point(3, 4)]) 长度为 2",
          "frozen 实例给属性赋值会抛 FrozenInstanceError"
        ],
        "reference": "from dataclasses import dataclass\n\n\n@dataclass(frozen=True)\nclass Point:\n    x: int\n    y: int\n\n\ndef unique_points(points):\n    return list(dict.fromkeys(points))"
      },
      "pitfalls": [
        "只重写 __eq__ 忘了 __hash__",
        "把可变对象当 dict 键",
        "以为哈希相同就代表相等",
        "把对象放进 set 后又改它的哈希字段"
      ]
    },
    {
      "code": "py.scope.legb",
      "title": "LEGB、global/nonlocal 与变量遮蔽",
      "minutes": 25,
      "module": "作用域",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1103",
          "relation": "introduces"
        },
        {
          "taskId": "1303",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "名字查找顺序是什么？global 和 nonlocal 各改变什么？",
        "answer": [
          "LEGB：Local -> Enclosing -> Global(模块) -> Builtins",
          "函数内只要出现赋值，该名字整个函数内都是局部变量（遮蔽外层）",
          "global name 声明后读写的是模块级变量；nonlocal name 指向最近的外层函数变量",
          "别用 global 传状态：参数进、返回值出更可测"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "x = \"global\"\n\n\ndef outer():\n    x = \"enclosing\"\n\n    def inner():\n        print(x)\n\n    inner()\n\n\nouter()\nprint(x)\n\ncount = 0\n\n\ndef bump():\n    global count\n    count += 1\n\n\nbump()\nprint(count)",
        "expected": [
          "enclosing",
          "global",
          "1"
        ],
        "explain": "inner 按 LEGB 先在 enclosing 找到 x；模块级 x 没被改动；global 声明让 bump 改的是模块级 count，所以打印 1。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "count = 0\n\n\ndef bump():\n    count += 1\n    return count\n\n\nprint(bump())",
        "rootCause": "函数内对 count 赋值使它整个函数内都是局部变量，+= 需要先读局部 count，于是抛 UnboundLocalError。",
        "fix": "确实要改模块级变量就加 global count；更好的做法是把计数放进类/闭包，或参数进返回值出。"
      },
      "code_task": {
        "prompt": "写 running_total(amount) 与 reset()：用模块级私有变量 _total + global 维护累计值，两个函数共享同一状态。",
        "acceptance": [
          "running_total(3) 返回 3，再 running_total(4) 返回 7",
          "reset() 之后 running_total(1) 返回 1",
          "状态放在模块级变量 _total 里，不用函数默认参数存",
          "不依赖调用顺序以外的隐藏全局（除 _total 外不新增全局）"
        ],
        "reference": "_total = 0\n\n\ndef running_total(amount):\n    global _total\n    _total += amount\n    return _total\n\n\ndef reset():\n    global _total\n    _total = 0"
      },
      "pitfalls": [
        "函数内赋值意外创建局部变量",
        "该用 nonlocal 却用 global",
        "用 global 在多个模块间传状态",
        "把内层便利变量与外层同名导致遮蔽"
      ]
    },
    {
      "code": "py.scope.closure",
      "title": "闭包与循环变量晚绑定",
      "minutes": 20,
      "module": "作用域",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1303",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "闭包捕获的是变量还是值？循环里创建函数为什么会全部返回最后一个值？",
        "answer": [
          "闭包按“名字”捕获外层变量，读的是调用那一刻的值（晚绑定）",
          "循环结束后循环变量停在最后一个值，所有闭包都看到它",
          "想固定当前值用默认参数（lambda x, i=i: ...）或工厂函数",
          "nonlocal 让内层函数能改写外层的可变绑定"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def make_adders():\n    return [lambda x: x + i for i in range(3)]\n\n\nprint([f(10) for f in make_adders()])\n\n\ndef make_adders_fixed():\n    funcs = []\n    for i in range(3):\n        funcs.append(lambda x, i=i: x + i)\n    return funcs\n\n\nprint([f(10) for f in make_adders_fixed()])",
        "expected": [
          "[12, 12, 12]",
          "[10, 11, 12]"
        ],
        "explain": "第一个版本的 lambda 都读同一个 i，调用时 i 已经是 2；第二个版本用默认参数 i=i 在定义时就绑定了 0/1/2。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def make_handlers():\n    handlers = {}\n    for name in [\"a\", \"b\"]:\n        handlers[name] = lambda: name\n    return handlers\n\n\nprint([handler() for handler in make_handlers().values()])",
        "rootCause": "所有 lambda 捕获的是同一个循环变量 name，循环结束后它停在 \"b\"，于是两个 handler 都返回 b。",
        "fix": "用默认参数固定当前值：lambda name=name: name，或把创建逻辑放进工厂函数 make_handler(name)。"
      },
      "code_task": {
        "prompt": "写 make_multipliers(factors)：返回函数列表，第 k 个函数把输入乘以 factors[k]，彼此独立、互不串值。",
        "acceptance": [
          "make_multipliers([0, 1, 2]) 的三个函数对 2 分别返回 0、2、4",
          "错开时间调用（先收集再统一调用）结果也不变",
          "factors 为空返回空列表",
          "用默认参数或工厂函数避免晚绑定"
        ],
        "reference": "def make_multipliers(factors):\n    return [lambda value, factor=factor: value * factor for factor in factors]"
      },
      "pitfalls": [
        "循环里创建闭包捕获同一个变量",
        "以为闭包捕获的是值快照",
        "在闭包外继续修改被捕获变量",
        "分不清 nonlocal 与 global 的作用对象"
      ]
    },
    {
      "code": "py.flow.iteration",
      "title": "for 迭代协议、range/enumerate/zip、边遍历边改",
      "minutes": 20,
      "module": "流程控制",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1301",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "for 循环背后发生了什么？边遍历边修改容器为什么危险？",
        "answer": [
          "for 依次调用 iter(对象) 的 __next__，直到 StopIteration",
          "range 只产生下标不产生列表；enumerate 同时给下标；zip 以最短序列为准",
          "迭代器是一次性的，遍历完就空了；可迭代对象可以重复取迭代器",
          "边遍历边增删会让索引错位，正确做法是倒序删或先收集再统一处理"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "for index, value in enumerate([\"a\", \"b\"], start=1):\n    print(index, value)\nfor left, right in zip([1, 2, 3], \"ab\"):\n    print(left, right)\nprint(list(range(2, 8, 3)))",
        "expected": [
          "1 a",
          "2 b",
          "1 a",
          "2 b",
          "[2, 5]"
        ],
        "explain": "enumerate 从 start=1 开始编号；zip 以最短的 \"ab\" 为准，第三个数字被丢弃；range(2, 8, 3) 产生 2 和 5。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "nums = [1, 2, 2, 3]\nfor value in nums:\n    if value == 2:\n        nums.remove(value)\nprint(nums)",
        "rootCause": "边遍历边删除导致索引错位：删掉一个 2 后后面的元素前移，迭代器却继续往后走，跳过了下一个 2。",
        "fix": "用新列表过滤（[n for n in nums if n != 2]），或收集要删的元素后统一删，或倒序遍历删除。"
      },
      "code_task": {
        "prompt": "写 pairwise(items)：返回相邻元素对列表，接受任意可迭代对象（含生成器），且不修改输入。",
        "acceptance": [
          "pairwise([1, 2, 3]) == [(1, 2), (2, 3)]",
          "元素少于 2 个时返回 []",
          "调用 pairwise 后原列表内容不变",
          "变体：pairwise(iter([1, 2, 3])) 同样返回 [(1, 2), (2, 3)]（生成器只遍历一次也能用）"
        ],
        "reference": "def pairwise(items):\n    values = list(items)\n    return [(values[index], values[index + 1]) for index in range(len(values) - 1)]"
      },
      "pitfalls": [
        "边遍历边修改容器",
        "zip 长度不等时以为会补齐",
        "对迭代器重复遍历拿到空结果",
        "用 range(len(x)) 却忘了边界"
      ]
    },
    {
      "code": "py.flow.while-break-else",
      "title": "while、break/continue 与循环 else",
      "minutes": 15,
      "module": "流程控制",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1301",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "while 和 for 怎么选？循环的 else 什么时候执行？",
        "answer": [
          "次数未知、靠条件收敛用 while；遍历序列用 for",
          "break 立即结束整个循环，continue 只跳过本次剩余语句",
          "循环 else 只在循环正常结束（没有 break）时执行，常用来写“没找到”分支",
          "while 必须在循环体内推进条件变量，否则死循环"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "n = 0\nwhile n < 3:\n    n += 1\n    if n == 2:\n        continue\n    print(n)\n\nfor value in [1, 3, 5]:\n    if value % 2 == 0:\n        print(\"有偶数\")\n        break\nelse:\n    print(\"全是奇数\")",
        "expected": [
          "1",
          "3",
          "全是奇数"
        ],
        "explain": "continue 跳过了 n == 2 的打印；for 循环没有遇到 break，所以执行 else 分支打印“全是奇数”。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "index = 0\nwhile index < 3:\n    print(index)\n    if index == 1:\n        index += 1",
        "rootCause": "只在 index == 1 时推进循环变量：index 为 0 时既不推进也不退出，循环条件永远成立，程序死循环。",
        "fix": "把 index += 1 移到循环体末尾无条件执行（或用 for 遍历 range(3)），保证每轮都朝出口前进。"
      },
      "code_task": {
        "prompt": "写 first_negative(nums)：返回第一个负数，没有则返回 None；用提前 return 或 for-else 实现，不使用额外标志位。",
        "acceptance": [
          "first_negative([1, -2, 3]) 返回 -2",
          "first_negative([1, 2]) 返回 None",
          "first_negative([]) 返回 None",
          "不依赖真值判断：0 不会被当成负数"
        ],
        "reference": "def first_negative(nums):\n    for value in nums:\n        if value < 0:\n            return value\n    return None"
      },
      "pitfalls": [
        "忘了推进 while 的条件变量",
        "else 绑错循环（缩进层级）",
        "以为 break 能跳出多层循环",
        "把循环 else 当 if-else 用"
      ]
    },
    {
      "code": "py.comprehension.basics",
      "title": "推导式与生成器表达式（含独立作用域）",
      "minutes": 25,
      "module": "流程控制",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1301",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "列表/字典/集合推导式怎么写？生成器表达式和它们有什么本质区别？",
        "answer": [
          "列表推导式 [表达式 for 元素 in 可迭代 if 条件]，字典用 {k: v ...}，集合用 {表达式 ...}",
          "生成器表达式用圆括号，惰性求值、只遍历一次、几乎不占内存",
          "Python 3 推导式有独立作用域，循环变量不会泄漏到外层",
          "嵌套推导式按书写顺序展开：先 for 外层，再 for 内层"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "nums = [1, 2, 3, 4]\nprint([n * n for n in nums if n % 2 == 0])\nprint({n: n * n for n in nums})\nprint(sorted({n % 2 for n in nums}))\ngen = (n for n in nums if n > 2)\nprint(list(gen), sum(gen))",
        "expected": [
          "[4, 16]",
          "{1: 1, 2: 4, 3: 9, 4: 16}",
          "[0, 1]",
          "[3, 4] 0"
        ],
        "explain": "四种形态输出一致；生成器表达式是一次性迭代器，list(gen) 已把它耗尽，再 sum(gen) 只能得到 0。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "nums = [1, 2, 3]\ngen = (n * 2 for n in nums)\nprint(sum(gen))\nprint(sum(gen))",
        "rootCause": "生成器表达式惰性且一次性，第一次 sum 已经耗尽它，第二次求和得到 0；它不会记住结果。",
        "fix": "需要重复使用就落地成 list（doubled = [n * 2 for n in nums]），或每次重新创建生成器表达式。"
      },
      "code_task": {
        "prompt": "写 normalize_names(names)：用一条推导式去掉首尾空白、转小写，并跳过去空白后为空的项。",
        "acceptance": [
          "normalize_names([\" A \", \"b\", \"  \"]) == [\"a\", \"b\"]",
          "返回新列表，原列表不变",
          "空列表返回 []",
          "用一条列表推导式实现（不写显式 for 循环）"
        ],
        "reference": "def normalize_names(names):\n    return [name.strip().lower() for name in names if name.strip()]"
      },
      "pitfalls": [
        "生成器表达式重复迭代",
        "在推导式里塞复杂副作用",
        "嵌套推导式的 for 顺序写反",
        "以为推导式会泄漏循环变量"
      ]
    },
    {
      "code": "py.flow.match-case",
      "title": "match/case 模式匹配与守卫",
      "minutes": 20,
      "module": "流程控制",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1505",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "match/case 和普通的 if-elif 有什么不同？写模式时要注意什么？",
        "answer": [
          "case 里是模式不是表达式：字面量、序列模式、映射模式、类模式、捕获变量、_ 通配",
          "捕获变量会绑定任何值，所以 case 变量的分支会吃掉后面所有分支",
          "if 守卫写在模式后：case [x, y] if x > y:",
          "匹配是自上而下取第一个成功项，顺序决定语义"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def describe(value):\n    match value:\n        case 0:\n            return \"零\"\n        case [x, y]:\n            return f\"两点 {x},{y}\"\n        case {\"type\": \"user\", **rest}:\n            return f\"用户 {sorted(rest)}\"\n        case _:\n            return \"其他\"\n\n\nprint(describe(0))\nprint(describe([1, 2]))\nprint(describe({\"type\": \"user\", \"name\": \"amy\"}))\nprint(describe(3.5))",
        "expected": [
          "零",
          "两点 1,2",
          "用户 ['name']",
          "其他"
        ],
        "explain": "0 命中字面量模式；列表命中序列模式并解包；dict 命中映射模式且 **rest 收集剩余键；3.5 落到通配 _。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def handle(command):\n    match command:\n        case [\"go\", steps]:\n            return steps\n        case [\"go\", steps] if steps > 0:\n            return steps * 2\n        case _:\n            return 0",
        "rootCause": "第一个 case 的捕获模式能匹配所有 [\"go\", 任意值]，它排在前面，带守卫的分支永远不可达，负数也走了第一条。",
        "fix": "把带守卫的分支放到前面，或给第一个 case 加守卫，让“更具体的模式”先匹配。"
      },
      "code_task": {
        "prompt": "写 parse_event(event)：用 match/case 处理 (\"click\", x, y)、(\"key\", name)、{\"kind\": \"scroll\", \"delta\": n} 三种输入，其他返回 None。",
        "acceptance": [
          "parse_event((\"click\", 1, 2)) == (\"click\", 1, 2)",
          "parse_event((\"key\", \"a\")) == (\"key\", \"a\")",
          "parse_event({\"kind\": \"scroll\", \"delta\": 5}) == (\"scroll\", 5)",
          "parse_event(\"nope\") is None 且不抛异常"
        ],
        "reference": "def parse_event(event):\n    match event:\n        case (\"click\", x, y):\n            return (\"click\", x, y)\n        case (\"key\", name):\n            return (\"key\", name)\n        case {\"kind\": \"scroll\", \"delta\": value}:\n            return (\"scroll\", value)\n        case _:\n            return None"
      },
      "pitfalls": [
        "通配/捕获分支放太前导致后面不可达",
        "在 case 里用变量名当常量（实际是捕获）",
        "守卫分支顺序写反",
        "把 match 当成会穿透的 switch"
      ]
    },
    {
      "code": "py.func.basics",
      "title": "定义、调用与返回值语义",
      "minutes": 10,
      "module": "函数",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1102",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "函数没有 return 时返回什么？print 和 return 有什么区别？",
        "answer": [
          "函数不写 return 或只写 return，返回 None",
          "print 把内容写到标准输出，return 把结果交给调用方，两者互不替代",
          "默认参数在定义时求值；参数是局部名，函数内的临时变量不会污染外部",
          "一个函数只做一件事，返回值类型保持稳定"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def greet(name, greeting=\"你好\"):\n    return f\"{greeting}，{name}\"\n\n\nprint(greet(\"Amy\"))\nprint(greet(\"Bob\", \"Hi\"))\nresult = greet(\"Cy\")\nprint(result, type(result).__name__)\n\n\ndef no_return():\n    print(\"执行了\")\n\n\nprint(no_return())",
        "expected": [
          "你好，Amy",
          "Hi，Bob",
          "你好，Cy str",
          "执行了",
          "None"
        ],
        "explain": "调用按位置传参覆盖默认值；greet 有 return 所以拿到 str；no_return 只打印，返回 None，外层 print 再打印一次 None。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def double(value):\n    value * 2\n\n\nprint(double(3))",
        "rootCause": "函数体只有表达式没有 return，计算结果被丢弃，函数返回 None。",
        "fix": "加 return：return value * 2。"
      },
      "code_task": {
        "prompt": "写 describe_number(n)：返回 \"负\"、\"零\"、\"正\" 三个字符串，每个分支都显式 return。",
        "acceptance": [
          "describe_number(-1) == \"负\"",
          "describe_number(0) == \"零\"",
          "describe_number(1) == \"正\"",
          "函数没有 print，返回值类型恒为 str"
        ],
        "reference": "def describe_number(n):\n    if n < 0:\n        return \"负\"\n    if n == 0:\n        return \"零\"\n    return \"正\""
      },
      "pitfalls": [
        "漏写 return（拿到 None）",
        "把 print 当返回值",
        "参数名与全局变量同名造成遮蔽",
        "默认参数用可变对象"
      ]
    },
    {
      "code": "py.func.signature",
      "title": "参数全解：位置/关键字/默认/*args/**kwargs/仅限/转发",
      "minutes": 30,
      "module": "函数",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1302",
          "relation": "introduces"
        },
        {
          "taskId": "1101",
          "relation": "exercises"
        }
      ],
      "concept": {
        "prompt": "一个函数签名的参数顺序规则是什么？/ 和 * 各起什么作用？",
        "answer": [
          "顺序：仅限位置 / 位置或关键字 / *args / 仅限关键字 / **kwargs",
          "调用时位置参数必须在关键字参数之前；一个参数不能传两次",
          "def f(base, /, path, *, secure=True)：/ 之前只能位置传，* 之后只能关键字传",
          "转发用 f(*args, **kwargs)，注意别把显式参数和 **kwargs 撞名"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def tag(text, prefix=\"[\", suffix=\"]\"):\n    return f\"{prefix}{text}{suffix}\"\n\n\nprint(tag(\"x\"))\nprint(tag(\"x\", suffix=\")\"))\nprint(tag(\"x\", \"(\", \")\"))\n\n\ndef collect(*args, **kwargs):\n    return args, sorted(kwargs.items())\n\n\nprint(collect(1, 2, a=3))",
        "expected": [
          "[x]",
          "[x)",
          "(x)",
          "((1, 2), [('a', 3)])"
        ],
        "explain": "默认值按声明顺序生效；关键字参数可单独覆盖 suffix；位置参数按顺序填 prefix/suffix；*args 收位置参数、**kwargs 收关键字参数。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def send(message, to, cc=None):\n    return f\"{message} -> {to}\"\n\n\nprint(send(\"hello\", cc=\"amy\"))",
        "rootCause": "to 没有默认值，是必需的位置或关键字参数；这次调用只给了 message 和 cc，缺 to，抛 TypeError。",
        "fix": "补上 to（send(\"hello\", \"amy\")），或把参数设计成都有默认值/用关键字强制传参并在函数里校验。"
      },
      "code_task": {
        "prompt": "写 build_url(base, /, path, *, secure=True, **params)：base/path 只能位置传参，secure 只能关键字传参，params 按插入顺序拼查询串。",
        "acceptance": [
          "build_url(\"example.com\", \"api\") == \"https://example.com/api\"",
          "build_url(\"example.com\", \"api\", secure=False) 用 http",
          "build_url(\"example.com\", \"api\", page=2, size=10) 得到 ?page=2&size=10",
          "用关键字传 base 会抛 TypeError（仅限位置参数）"
        ],
        "reference": "def build_url(base, /, path, *, secure=True, **params):\n    scheme = \"https\" if secure else \"http\"\n    query = \"&\".join(f\"{key}={value}\" for key, value in params.items())\n    url = f\"{scheme}://{base}/{path}\"\n    return f\"{url}?{query}\" if query else url"
      },
      "pitfalls": [
        "位置参数与关键字参数顺序冲突",
        "以为 *args 后面的参数还能位置传",
        "可变对象当默认值",
        "**kwargs 与显式参数撞名导致 TypeError"
      ]
    },
    {
      "code": "py.func.functional",
      "title": "一等函数、lambda 与 map/filter/sorted-key",
      "minutes": 25,
      "module": "函数",
      "level": "基础",
      "taskRefs": [],
      "concept": {
        "prompt": "函数为什么能像数据一样传递？lambda 适合用在哪、不适合用在哪？",
        "answer": [
          "函数是一等对象：可以赋值、当参数、当返回值、放进容器",
          "lambda 是只能写一个表达式的匿名函数，适合短小的 key/回调",
          "map/filter 返回惰性迭代器，要 list 得显式转换；sorted(key=...) 是最常用的函数式接口",
          "复杂逻辑用 def 更可读，也更好调试和复用"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "nums = [1, 2, 3, 4]\nprint(list(map(lambda n: n * 2, nums)))\nprint(list(filter(lambda n: n % 2 == 0, nums)))\nprint(sorted(nums, key=lambda n: -n))\nfrom functools import reduce\nprint(reduce(lambda a, b: a + b, nums, 0))",
        "expected": [
          "[2, 4, 6, 8]",
          "[2, 4]",
          "[4, 3, 2, 1]",
          "10"
        ],
        "explain": "map/filter 是惰性的，用 list 取出结果；key 取负实现降序；reduce 带初值 0 累加得到 10。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "nums = [1, 2, 3]\nresult = map(lambda n: n * 2, nums)\nprint(list(result))\nprint(len(result))",
        "rootCause": "map 返回的是一次性迭代器：list(result) 已经把它耗尽，而且迭代器没有长度，len(result) 直接抛 TypeError。",
        "fix": "需要多次使用或求长度就先落地：doubled = list(map(...))，之后用 doubled 做所有操作。"
      },
      "code_task": {
        "prompt": "写 compose(*funcs)：返回一个组合函数，按从右到左的顺序依次应用；不传函数时返回恒等函数。",
        "acceptance": [
          "compose()(5) == 5",
          "compose(lambda x: x + 1, lambda x: x * 2)(3) == 7（先 *2 再 +1）",
          "支持任意多个函数，顺序为从右到左",
          "不修改传入的函数，也不依赖全局状态"
        ],
        "reference": "def compose(*funcs):\n    def composed(value):\n        for func in reversed(funcs):\n            value = func(value)\n        return value\n\n    return composed"
      },
      "pitfalls": [
        "把 map/filter 的迭代器当列表反复用",
        "lambda 里塞多步逻辑",
        "忘记 list() 导致打印出 <map object>",
        "sorted 的 key 返回了不可比较的类型"
      ]
    },
    {
      "code": "py.func.recursion",
      "title": "递归、基准情形与递归深度",
      "minutes": 20,
      "module": "函数",
      "level": "基础",
      "taskRefs": [],
      "concept": {
        "prompt": "写递归必须保证什么？什么时候不该用递归？",
        "answer": [
          "必须有基准情形（最小的、直接可解的情况），且每次递归都朝它靠近",
          "递归适合树/嵌套结构；线性累积用迭代更省栈",
          "Python 默认递归深度约 1000，超了抛 RecursionError",
          "能用循环或显式栈替代时优先替代，或加缓存避免重复子问题"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def fact(n):\n    if n <= 1:\n        return 1\n    return n * fact(n - 1)\n\n\nprint(fact(5))\n\n\ndef flatten(items):\n    result = []\n    for item in items:\n        if isinstance(item, list):\n            result.extend(flatten(item))\n        else:\n            result.append(item)\n    return result\n\n\nprint(flatten([1, [2, [3, 4]], 5]))",
        "expected": [
          "120",
          "[1, 2, 3, 4, 5]"
        ],
        "explain": "fact 的基准是 n <= 1；flatten 对 list 递归、对普通元素直接收集，能处理任意深度嵌套。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def countdown(n):\n    print(n)\n    if n == 0:\n        return\n    countdown(n - 1)\n\n\ncountdown(-1)",
        "rootCause": "基准情形只判断 n == 0，负数从 -1 开始每次减 1 只会越来越小，永远到不了 0，最终 RecursionError。",
        "fix": "基准情形要覆盖边界：改成 if n <= 0: return，并在入口校验 n 必须是整数。"
      },
      "code_task": {
        "prompt": "写 depth(value)：返回嵌套列表的最大深度；非 list 视为第 0 层，空 list 深度为 1。",
        "acceptance": [
          "depth([]) == 1",
          "depth([1, 2]) == 1",
          "depth([1, [2, [3]]]) == 3",
          "变体：depth([[[]]]) == 3（中间层为空列表也按一层算）"
        ],
        "reference": "def depth(value):\n    if not isinstance(value, list):\n        return 0\n    if not value:\n        return 1\n    return 1 + max(depth(item) for item in value)"
      },
      "pitfalls": [
        "基准情形缺失或写错方向",
        "递归深度超限",
        "重复子问题没有记忆化",
        "对线性数据也用递归导致爆栈"
      ]
    },
    {
      "code": "py.func.pure-refactor",
      "title": "纯函数、副作用与重构边界",
      "minutes": 20,
      "module": "函数",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1304",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "什么是纯函数？为什么它更好测、更好重构？",
        "answer": [
          "纯函数：相同输入必得相同输出，且不产生可观察副作用（不改入参、不碰全局、不做 IO）",
          "副作用包括：修改入参、写全局/文件/网络、打印、依赖时间与随机数",
          "重构边界：把计算与 IO/状态分开，计算部分保持纯，IO 放在外层",
          "纯函数不意味着复制一切：不改入参即可，必要时返回新对象"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def add_item(items, item):\n    return items + [item]\n\n\noriginal = [1]\nupdated = add_item(original, 2)\nprint(original, updated)\n\ntotal = 0\n\n\ndef impure(n):\n    global total\n    total += n\n    return total\n\n\nprint(impure(1), impure(2), total)",
        "expected": [
          "[1] [1, 2]",
          "1 3 3"
        ],
        "explain": "add_item 返回新列表，original 不受影响；impure 依赖并修改全局 total，所以两次调用结果不同，最后 total 是 3。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def apply_discount(prices, rate):\n    for index, price in enumerate(prices):\n        prices[index] = price * (1 - rate)\n    return prices\n\n\nprices = [100, 200]\nprint(apply_discount(prices, 0.1))\nprint(prices)",
        "rootCause": "函数原地改写了调用方的列表，是隐藏副作用：再调用一次会重复打折，别的持有者也会莫名看到变化。",
        "fix": "改成纯函数返回新列表：return [price * (1 - rate) for price in prices]；需要原地更新时由调用方显式决定。"
      },
      "code_task": {
        "prompt": "写纯函数 summarize(lines)：返回 (行数, 去重后的单词数)，不打印、不修改入参、不读写全局与文件。",
        "acceptance": [
          "summarize([\"a b\", \"b c\"]) == (2, 3)",
          "summarize([]) == (0, 0)",
          "调用后入参列表内容不变",
          "函数体内没有任何 print/open/global 语句"
        ],
        "reference": "def summarize(lines):\n    words = set()\n    for line in lines:\n        words.update(line.split())\n    return len(lines), len(words)"
      },
      "pitfalls": [
        "函数偷偷修改入参",
        "依赖全局状态导致结果不稳定",
        "把打印/IO 混进计算函数",
        "为了“纯”而无脑深拷贝大对象"
      ]
    },
    {
      "code": "py.exc.flow",
      "title": "try/except/else/finally、异常链与 traceback",
      "minutes": 25,
      "module": "异常",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1401",
          "relation": "introduces"
        },
        {
          "taskId": "1101",
          "relation": "exercises"
        }
      ],
      "concept": {
        "prompt": "try 的四个子句各自什么时候执行？异常链怎么保留？",
        "answer": [
          "try 里的代码抛错后跳到匹配的 except；没抛错则执行 else",
          "finally 无论是否异常、是否 return 都会执行，常用来释放资源",
          "finally 里 return 会吞掉正在传播的异常，绝大多数情况都不该这么写",
          "raise X from error 保留原始异常链，traceback 里能看到“The above exception was the direct cause...”"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def divide(a, b):\n    try:\n        result = a / b\n    except ZeroDivisionError:\n        return \"除零\"\n    else:\n        return result\n    finally:\n        print(\"清理\")\n\n\nprint(divide(6, 2))\nprint(divide(1, 0))",
        "expected": [
          "清理",
          "3.0",
          "清理",
          "除零"
        ],
        "explain": "两次调用都会执行 finally 的打印（即使 return 也会先跑 finally）；正常路径返回 3.0，除零路径返回“除零”。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "try:\n    value = int(\"abc\")\nexcept Exception:\n    print(\"通用错误\")\nexcept ValueError:\n    print(\"值错误\")",
        "rootCause": "except 从上往下匹配，Exception 在前会先接住 ValueError，后面的具体分支永远不可达；静态检查也会提示 unreachable。",
        "fix": "把具体异常写在前面、宽泛的 Exception 放最后；能只捕获 ValueError 就不要捕获 Exception。"
      },
      "code_task": {
        "prompt": "写 safe_parse(text)：合法整数返回 int；失败时抛自定义 ParseError，并用 raise ... from 保留原始 ValueError。",
        "acceptance": [
          "safe_parse(\"42\") == 42",
          "safe_parse(\"abc\") 抛 ParseError",
          "ParseError.__cause__ 是原始的 ValueError",
          "finally 子句里不写 return（避免吞异常）"
        ],
        "reference": "class ParseError(ValueError):\n    pass\n\n\ndef safe_parse(text):\n    try:\n        return int(text)\n    except ValueError as error:\n        raise ParseError(f\"无法解析：{text!r}\") from error"
      },
      "pitfalls": [
        "finally 里 return 吞掉异常",
        "except 顺序从宽到窄",
        "except 之后继续使用未赋值的变量",
        "把 try 包得过大掩盖真正出错的位置"
      ]
    },
    {
      "code": "py.exc.types",
      "title": "异常层次、自定义异常与 raise",
      "minutes": 25,
      "module": "异常",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1401",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "内置异常层次是怎样的？自定义异常该继承谁？",
        "answer": [
          "BaseException -> Exception -> 具体异常；KeyboardInterrupt/SystemExit 属于 BaseException",
          "LookupError 是 KeyError/IndexError 的父类；OSError 涵盖文件与系统错误",
          "自定义业务异常继承 Exception（或更具体的父类），别继承 BaseException",
          "raise 只接受异常类或实例，Python 3 不允许 raise 字符串"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "try:\n    raise KeyError(\"缺失\")\nexcept LookupError as error:\n    print(type(error).__name__, error.args)\n\n\nclass ConfigError(ValueError):\n    pass\n\n\ntry:\n    raise ConfigError(\"配置无效\")\nexcept ValueError as error:\n    print(isinstance(error, ConfigError), str(error))",
        "expected": [
          "KeyError ('缺失',)",
          "True 配置无效"
        ],
        "explain": "KeyError 是 LookupError 的子类所以能被捕获；ConfigError 继承 ValueError，用父类也能捕获，isinstance 仍为 True。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "class Cancel(BaseException):\n    pass\n\n\ndef run():\n    try:\n        raise Cancel(\"用户取消\")\n    except Exception:\n        return \"被吞掉了\"\n    return \"ok\"\n\n\nprint(run())",
        "rootCause": "Cancel 继承 BaseException 而不是 Exception，except Exception 捕不到它，异常会直接向上冒泡，想“兜底所有业务异常”的调用方全都接不住。",
        "fix": "自定义异常应继承 Exception（如 class Cancel(Exception)），除非确实需要绕过常规捕获（如系统级退出信号）。"
      },
      "code_task": {
        "prompt": "定义 AppError -> NotFound -> UserNotFound 层次，写 get_user(users, uid)：找不到抛 UserNotFound，消息里带上 uid。",
        "acceptance": [
          "get_user({1: \"amy\"}, 1) == \"amy\"",
          "get_user({}, 2) 抛 UserNotFound",
          "UserNotFound 是 AppError 的子类，可用 except AppError 统一捕获",
          "异常消息包含 uid，且 __cause__ 指向原始 KeyError"
        ],
        "reference": "class AppError(Exception):\n    pass\n\n\nclass NotFound(AppError):\n    pass\n\n\nclass UserNotFound(NotFound):\n    pass\n\n\ndef get_user(users, uid):\n    try:\n        return users[uid]\n    except KeyError as error:\n        raise UserNotFound(f\"用户不存在：{uid}\") from error"
      },
      "pitfalls": [
        "自定义异常继承 BaseException",
        "异常类名与内置异常冲突",
        "raise 字符串（Python 3 非法）",
        "用异常做正常流程控制"
      ]
    },
    {
      "code": "py.exc.over-catch",
      "title": "捕获范围过大与异常吞噬",
      "minutes": 15,
      "module": "异常",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1103",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "为什么 except Exception: pass 很危险？该怎么收窄捕获？",
        "answer": [
          "捕获过宽会把真正的 bug（拼写错误、类型错误、权限问题）一起吞掉，问题被推迟到更难排查的地方",
          "只捕获你确实能处理的异常，其余让它向上冒泡",
          "捕获后要么恢复、要么记录日志并重抛，不能静默 pass",
          "except Exception 会拦住 KeyboardInterrupt 之外几乎所有错误，通常只该出现在最外层兜底并记录"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def parse(value):\n    try:\n        return int(value)\n    except Exception:\n        return None\n\n\nprint(parse(\"12\"))\nprint(parse(\"abc\"))\n\ntry:\n    \"abc\" + 1\nexcept (TypeError, ValueError) as error:\n    print(type(error).__name__)",
        "expected": [
          "12",
          "None",
          "TypeError"
        ],
        "explain": "parse 把异常转成 None；第二段只捕获明确的 TypeError/ValueError，打印出实际异常类型。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def save(path, text):\n    try:\n        with open(path, \"w\") as handle:\n            handle.write(text)\n    except Exception:\n        pass",
        "rootCause": "裸 except Exception + pass 把权限不足、磁盘写满、编码错误全部吃掉，调用方以为保存成功，数据却丢了。",
        "fix": "只捕获明确的 IOError/OSError，记录日志后重新抛出（或返回明确的失败结果），其余异常不要拦。"
      },
      "code_task": {
        "prompt": "写 load_config(path)：文件不存在时返回 {}，其他 OSError 继续抛；JSON 解析失败抛带路径的 ValueError；不使用裸 except。",
        "acceptance": [
          "路径不存在时返回 {}",
          "目录等非文件路径抛 OSError（不被当成“默认配置”）",
          "内容不是合法 JSON 时抛 ValueError 且消息含路径",
          "代码中不出现裸 except / except Exception"
        ],
        "reference": "import json\nfrom pathlib import Path\n\n\ndef load_config(path):\n    file_path = Path(path)\n    try:\n        text = file_path.read_text(encoding=\"utf-8\")\n    except FileNotFoundError:\n        return {}\n    try:\n        return json.loads(text)\n    except json.JSONDecodeError as error:\n        raise ValueError(f\"配置不是合法 JSON：{file_path}\") from error"
      },
      "pitfalls": [
        "裸 except 吞异常",
        "except Exception 后直接 pass",
        "连 KeyboardInterrupt/SystemExit 一起捕获",
        "捕获后既不记录也不重抛"
      ]
    },
    {
      "code": "py.exc.eafp-assert",
      "title": "EAFP vs LBYL、assert 的边界",
      "minutes": 15,
      "module": "异常",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1502",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "EAFP 和 LBYL 分别是什么？assert 能用来校验参数吗？",
        "answer": [
          "LBYL：先 if key in data 再取；EAFP：直接取，捕获 KeyError",
          "EAFP 在并发/文件系统场景更安全：检查和使用之间状态可能变",
          "assert 是调试工具，python -O 会整体移除，绝不能用于校验外部输入或业务规则",
          "对外部输入用 if + raise ValueError，对内部不变量才用 assert"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "data = {\"name\": \"amy\"}\ntry:\n    age = data[\"age\"]\nexcept KeyError:\n    age = 0\nprint(age)\n\nvalue = data.get(\"age\")\nif value is None:\n    value = 0\nprint(value)\n\nassert value == 0, \"value 应为 0\"\nprint(\"断言通过\")",
        "expected": [
          "0",
          "0",
          "断言通过"
        ],
        "explain": "EAFP 用 try/except 兜底缺失键；LBYL 用 get + None 判断；断言成立所以继续执行。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def withdraw(balance, amount):\n    assert amount <= balance, \"余额不足\"\n    return balance - amount\n\n\nprint(withdraw(100, 50))\nprint(withdraw(100, 200))",
        "rootCause": "用 assert 做业务校验：python -O 会去掉所有断言，余额不足也会直接扣钱；断言消息也不适合作为对外错误契约。",
        "fix": "改成显式校验并抛业务异常：if amount > balance: raise ValueError(\"余额不足\")；assert 只留给内部不变量。"
      },
      "code_task": {
        "prompt": "写 read_field(data, key, default) 用成员判断区分“键不存在”和“值是 None”，并写 require_positive(value) 用异常而非 assert 校验参数。",
        "acceptance": [
          "read_field({\"a\": None}, \"a\", 0) 返回 None 而不是 0",
          "read_field({}, \"a\", 0) 返回 0",
          "require_positive(0) 抛 ValueError",
          "两个函数里都不使用 assert"
        ],
        "reference": "def read_field(data, key, default):\n    if key in data:\n        return data[key]\n    return default\n\n\ndef require_positive(value):\n    if value <= 0:\n        raise ValueError(f\"value 必须为正：{value}\")\n    return value"
      },
      "pitfalls": [
        "用 assert 校验用户输入",
        "依赖断言在 -O 下仍然生效",
        "LBYL 与 EAFP 混用导致重复检查",
        "把异常当正常控制流滥用"
      ]
    },
    {
      "code": "py.io.files",
      "title": "文件读写：模式、文本/二进制与 with",
      "minutes": 25,
      "module": "文件与IO",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1501",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "open 的模式怎么选？为什么必须用 with？",
        "answer": [
          "r 读、w 覆盖写、a 追加、x 独占创建、+ 读写；b 表示二进制",
          "文本模式按 encoding 解码成 str；二进制模式读写 bytes，适合图片等非文本",
          "with open(...) as f 保证异常时也关闭文件，手动 close 容易漏",
          "写文本记得显式指定 encoding=\"utf-8\"，别依赖平台默认编码"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "from pathlib import Path\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as work:\n    path = Path(work) / \"note.txt\"\n    path.write_text(\"第一行\\n第二行\\n\", encoding=\"utf-8\")\n    print(path.read_text(encoding=\"utf-8\").splitlines())\n    with path.open(\"a\", encoding=\"utf-8\") as handle:\n        handle.write(\"第三行\\n\")\n    print(len(path.read_text(encoding=\"utf-8\").splitlines()))\n    with path.open(\"rb\") as handle:\n        print(handle.read(3) == \"第\".encode(\"utf-8\"))",
        "expected": [
          "['第一行', '第二行']",
          "3",
          "True"
        ],
        "explain": "splitlines 按换行切成两行；a 模式追加后共三行；二进制模式读出的前 3 字节正是“第”的 UTF-8 编码。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "handle = open(\"data.txt\", \"w\")\nhandle.write(\"hello\")",
        "rootCause": "手动 open 没有 close：数据可能长时间不落盘，文件句柄也会泄漏，异常时更是永远关不掉。",
        "fix": "用上下文管理器：with open(\"data.txt\", \"w\", encoding=\"utf-8\") as handle: handle.write(\"hello\")。"
      },
      "code_task": {
        "prompt": "写 append_lines(path, lines) 与 count_lines(path)：以 UTF-8 追加多行（每行以换行结尾，文件不存在则创建），并能数出行数。",
        "acceptance": [
          "文件不存在时 append_lines 会创建它",
          "追加不覆盖已有内容",
          "每写一行都以 \\n 结尾",
          "count_lines 能正确数出追加后的行数"
        ],
        "reference": "from pathlib import Path\n\n\ndef append_lines(path, lines):\n    with Path(path).open(\"a\", encoding=\"utf-8\") as handle:\n        for line in lines:\n            handle.write(f\"{line}\\n\")\n\n\ndef count_lines(path):\n    with Path(path).open(encoding=\"utf-8\") as handle:\n        return sum(1 for _ in handle)"
      },
      "pitfalls": [
        "忘记关闭文件",
        "文本模式写 bytes",
        "相对路径依赖当前工作目录",
        "不指定编码导致跨平台乱码"
      ]
    },
    {
      "code": "py.json.basics",
      "title": "JSON 序列化与类型映射",
      "minutes": 20,
      "module": "文件与IO",
      "level": "基础",
      "taskRefs": [],
      "concept": {
        "prompt": "Python 对象和 JSON 之间怎么映射？哪些类型不能直接序列化？",
        "answer": [
          "dict<->object、list/tuple->array、str->string、int/float->number、True/False->true/false、None->null",
          "tuple 会被序列化成数组，反序列化回来是 list，往返类型不守恒",
          "set、datetime、自定义对象不能直接 dump，需要用 default= 转换",
          "ensure_ascii=False 保留中文，indent 让文件可读，sort_keys 让输出稳定便于 diff"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "import json\n\npayload = {\"name\": \"amy\", \"tags\": [\"a\", \"b\"], \"age\": 3, \"ok\": True}\ntext = json.dumps(payload, ensure_ascii=False, sort_keys=True)\nprint(text)\nprint(json.loads(text) == payload)\nprint(json.dumps({\"a\": (1, 2)}))\nprint(json.dumps({\"a\": None}))",
        "expected": [
          "{\"age\": 3, \"name\": \"amy\", \"ok\": true, \"tags\": [\"a\", \"b\"]}",
          "True",
          "{\"a\": [1, 2]}",
          "{\"a\": null}"
        ],
        "explain": "sort_keys 让键按字母序输出；True 序列化为 true 再解析回 True 所以相等；tuple 变数组；None 变 null。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "import json\n\ndata = json.loads('{\"count\": \"3\"}')\nprint(data[\"count\"] + 1)",
        "rootCause": "JSON 里数字写成带引号的字符串时，反序列化后仍是 str，直接 + 1 抛 TypeError；JSON 类型不会自动转成想要的 Python 类型。",
        "fix": "按契约校验并显式转换：count = int(data[\"count\"])，转换失败按业务处理，别假设 JSON 字段类型。"
      },
      "code_task": {
        "prompt": "写 save_json(path, payload) 与 load_json(path)：UTF-8、ensure_ascii=False、缩进 2；加载非法 JSON 时抛带路径的 ValueError。",
        "acceptance": [
          "save_json 后再 load_json 得到与原始 payload 相等的数据",
          "文件里中文以原字符出现（不出现 \\uXXXX 转义）",
          "文件使用 2 空格缩进",
          "变体：内容非法时 load_json 抛 ValueError，且消息里包含文件路径"
        ],
        "reference": "import json\nfrom pathlib import Path\n\n\ndef save_json(path, payload):\n    text = json.dumps(payload, ensure_ascii=False, indent=2)\n    Path(path).write_text(text, encoding=\"utf-8\")\n\n\ndef load_json(path):\n    file_path = Path(path)\n    try:\n        return json.loads(file_path.read_text(encoding=\"utf-8\"))\n    except json.JSONDecodeError as error:\n        raise ValueError(f\"不是合法 JSON：{file_path}\") from error"
      },
      "pitfalls": [
        "以为 tuple/set 能往返保类型",
        "datetime 不转字符串直接 dump",
        "忘了 ensure_ascii=False 导致中文全变转义",
        "混淆 JSON 数字与字符串"
      ]
    },
    {
      "code": "py.path.pathlib",
      "title": "pathlib、目录遍历与文件元数据",
      "minutes": 30,
      "module": "文件与IO",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1501",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "pathlib 比字符串拼路径好在哪？iterdir/glob/rglob 有什么区别？",
        "answer": [
          "Path 用 / 拼路径、跨平台，且把路径当对象而不是字符串",
          "iterdir 列直接子项；glob 按模式匹配当前层；rglob 递归匹配",
          "name/suffix/stem/parent 取路径各部分，stat() 取大小与时间等元数据",
          "写文件前先 parent.mkdir(parents=True, exist_ok=True)，别假设目录存在"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "from pathlib import Path\nimport tempfile\n\nwith tempfile.TemporaryDirectory() as work:\n    root = Path(work)\n    (root / \"a.txt\").write_text(\"a\", encoding=\"utf-8\")\n    (root / \"sub\").mkdir()\n    (root / \"sub\" / \"b.txt\").write_text(\"b\", encoding=\"utf-8\")\n    print(sorted(p.name for p in root.iterdir()))\n    print(sorted(p.name for p in root.rglob(\"*.txt\")))\n    info = (root / \"a.txt\").stat()\n    print(info.st_size)\n    print((root / \"a.txt\").suffix, (root / \"a.txt\").stem)",
        "expected": [
          "['a.txt', 'sub']",
          "['a.txt', 'b.txt']",
          "1",
          ".txt a"
        ],
        "explain": "iterdir 列出直接子项（含子目录）；rglob 递归找所有 .txt；stat().st_size 是字节数；suffix 带点、stem 是不含后缀的文件名。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "from pathlib import Path\n\npath = Path(\"data\") / \"notes.txt\"\npath.write_text(\"hi\", encoding=\"utf-8\")",
        "rootCause": "父目录 data 不存在时 write_text 不会自动创建目录，直接抛 FileNotFoundError。",
        "fix": "先建目录：path.parent.mkdir(parents=True, exist_ok=True)，再写文件。"
      },
      "code_task": {
        "prompt": "写 list_files(root, suffix)：递归找出所有指定后缀的普通文件，返回按字符串排序的 Path 列表；root 不是目录时抛 FileNotFoundError。",
        "acceptance": [
          "递归包含子目录里的文件",
          "只返回指定后缀的文件（过滤掉目录）",
          "结果按路径字符串排序，稳定可预期",
          "root 不存在或是文件时抛 FileNotFoundError"
        ],
        "reference": "from pathlib import Path\n\n\ndef list_files(root, suffix):\n    root_path = Path(root)\n    if not root_path.is_dir():\n        raise FileNotFoundError(f\"目录不存在：{root_path}\")\n    found = (path for path in root_path.rglob(f\"*{suffix}\") if path.is_file())\n    return sorted(found, key=lambda path: str(path))"
      },
      "pitfalls": [
        "用字符串拼接路径",
        "忘记 mkdir 父目录",
        "glob 与 rglob 混用导致漏掉子目录",
        "对超大目录一次性读进内存"
      ]
    },
    {
      "code": "py.import.basics",
      "title": "import 机制、from-import 与 __main__",
      "minutes": 25,
      "module": "模块与导入",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1402",
          "relation": "introduces"
        },
        {
          "taskId": "1101",
          "relation": "exercises"
        }
      ],
      "concept": {
        "prompt": "import 做了什么？__name__ == \"__main__\" 有什么用？",
        "answer": [
          "import 执行模块顶层代码并把模块对象绑定到名字，模块缓存在 sys.modules 里只加载一次",
          "from x import y 只绑定 y，命名空间更干净，但名字被遮蔽时不好排查",
          "importlib.import_module 支持运行时按字符串导入",
          "__name__ 在直接运行脚本时是 \"__main__\"，被 import 时是模块名；入口逻辑要放进 if __name__ == \"__main__\":"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "import json\nfrom json import dumps as to_text\nfrom pathlib import Path\n\nprint(json.dumps([1]) == to_text([1]))\nprint(Path(\"a/b\").name)\nprint(__name__)",
        "expected": [
          "True",
          "b",
          "__main__"
        ],
        "explain": "两种导入方式指向同一个函数；Path.name 取最后一段；脚本直接运行时 __name__ 是 \"__main__\"。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "import json\n\n\ndef load(text):\n    return json.loads(text)\n\n\nload(\"{bad}\")",
        "rootCause": "模块顶层直接执行解析，别人 import 这个模块时会立刻抛 JSONDecodeError，模块无法被复用和测试。",
        "fix": "把可执行逻辑放进函数并保护入口：if __name__ == \"__main__\": main()，import 时只定义不执行。"
      },
      "code_task": {
        "prompt": "写 safe_import_name(module_name)：用 importlib 按名字导入并返回模块对象，导入失败返回 None，不吞 KeyboardInterrupt。",
        "acceptance": [
          "safe_import_name(\"json\") 返回模块对象",
          "不存在的模块名返回 None",
          "KeyboardInterrupt 不会被吞掉（只捕获 ImportError）",
          "变体：safe_import_name(\"json.decoder\") 也能返回子模块"
        ],
        "reference": "import importlib\n\n\ndef safe_import_name(module_name):\n    try:\n        return importlib.import_module(module_name)\n    except ImportError:\n        return None"
      },
      "pitfalls": [
        "from x import * 污染命名空间",
        "模块顶层写副作用代码",
        "循环导入",
        "没有包上下文时用相对导入"
      ]
    },
    {
      "code": "py.import.packages",
      "title": "包、相对导入与循环导入",
      "minutes": 25,
      "module": "模块与导入",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1402",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "包是什么？相对导入和循环导入要注意什么？",
        "answer": [
          "__init__.py 标记包目录，也可以承载包级导出；子模块用 pkg.mod 形式导入",
          "相对导入 . 表示当前包、.. 表示上级包，只能在包内使用",
          "循环导入会让模块在未初始化完时被对方读取，抛 ImportError 或拿到半成品",
          "破解循环导入：下沉重构到第三个模块、延迟到函数内导入、或用 TYPE_CHECKING 只做类型导入"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "import json\nfrom collections import OrderedDict\n\nprint(json.__name__)\nprint(OrderedDict.__module__)\nprint(hasattr(json, \"loads\"), hasattr(json, \"no_such\"))",
        "expected": [
          "json",
          "collections",
          "True False"
        ],
        "explain": "模块对象有 __name__；类型/函数的 __module__ 指向定义它的模块；hasattr 可以用来探测可选接口。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "# pkg/a.py\nfrom pkg.b import helper\n\n\ndef run():\n    return helper()\n\n\n# pkg/b.py\nfrom pkg.a import run\n\n\ndef helper():\n    return \"ok\"",
        "rootCause": "a 与 b 互相导入：先导入 a 时 b 又会去导入 a，此时 a 的 run 还没定义，导入直接失败（或拿到不完整的模块）。",
        "fix": "把共享逻辑下沉到独立的第三个模块，或把导入放进函数内部延迟执行，只保留顶层的类型导入。"
      },
      "code_task": {
        "prompt": "写 load_module_from_path(path, name=\"loaded_module\")：用 importlib.util 从任意 .py 文件加载模块，路径不是文件时返回 None。",
        "acceptance": [
          "加载存在的 .py 文件后能调用它里面的函数",
          "路径不存在时返回 None",
          "spec 或 loader 缺失时返回 None",
          "加载失败不污染调用方命名空间（返回模块对象而不是直接 exec）"
        ],
        "reference": "import importlib.util\nfrom pathlib import Path\n\n\ndef load_module_from_path(path, name=\"loaded_module\"):\n    file_path = Path(path)\n    if not file_path.is_file():\n        return None\n    spec = importlib.util.spec_from_file_location(name, file_path)\n    if spec is None or spec.loader is None:\n        return None\n    module = importlib.util.module_from_spec(spec)\n    spec.loader.exec_module(module)\n    return module"
      },
      "pitfalls": [
        "循环导入",
        "没有 __init__.py 就用相对导入",
        "把可执行脚本塞进包顶层",
        "__init__.py 里写重逻辑导致 import 变慢"
      ]
    },
    {
      "code": "py.env.setup",
      "title": "venv、依赖记录与 pyproject/src 布局",
      "minutes": 30,
      "module": "工程化",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1403",
          "relation": "introduces"
        },
        {
          "taskId": "1404",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "为什么要用虚拟环境？依赖该记在哪里？",
        "answer": [
          "venv 为项目隔离解释器与第三方包，避免全局互相污染",
          "依赖声明写进 requirements.txt 或 pyproject.toml 的 dependencies，环境才可重建",
          "pyproject.toml 是现代标准入口（build-system / project / 工具配置），src 布局避免误导入",
          "venv 目录、缓存、密钥不能提交进仓库；版本要能锁定"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "import sys, os\nfrom pathlib import Path\n\nprint(sys.version_info.major)\nprint(bool(sys.executable))\nprint(Path(\"pyproject.toml\").name)\nprint(os.environ.get(\"NOT_SET_VALUE\", \"默认\"))",
        "expected": [
          "3",
          "True",
          "pyproject.toml",
          "默认"
        ],
        "explain": "当前解释器主版本是 3；sys.executable 指向正在运行的 Python；Path 只解析路径不检查存在；环境变量缺失时 get 返回默认值。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "import pandas",
        "rootCause": "直接 import 第三方库却不在依赖文件里声明，本地能跑是因为碰巧装过；换环境或 CI 立刻 ImportError。",
        "fix": "在 requirements.txt / pyproject.toml 里声明依赖并在 venv 中安装；提交锁定的版本，避免“我机器上能跑”。"
      },
      "code_task": {
        "prompt": "写 parse_requirements(text)：解析依赖清单文本，忽略空行与 # 注释、去掉行内注释与首尾空白，返回包名列表（保留 == 等版本约束）。",
        "acceptance": [
          "空行与纯注释行被忽略",
          "行内注释（flask==2.0  # web）被去掉",
          "版本约束 ==、>= 等原样保留",
          "返回顺序与原文一致"
        ],
        "reference": "def parse_requirements(text):\n    packages = []\n    for raw in text.splitlines():\n        line = raw.split(\"#\", 1)[0].strip()\n        if line:\n            packages.append(line)\n    return packages"
      },
      "pitfalls": [
        "不建 venv，全局装包",
        "requirements 与 pyproject 声明不一致",
        "把 venv 目录提交进仓库",
        "依赖不锁版本导致构建漂移"
      ]
    },
    {
      "code": "py.test.pytest",
      "title": "pytest 基础与边界用例",
      "minutes": 20,
      "module": "工程化",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1204",
          "relation": "introduces"
        },
        {
          "taskId": "1404",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "好测试长什么样？正常路径、边界、异常路径各要怎么覆盖？",
        "answer": [
          "一个测试只验证一件事，名字说清场景（test_parse_int_rejects_empty）",
          "先写失败用例；正常值、边界值（空、0、极值）、异常路径都要覆盖",
          "异常路径用 pytest.raises / assertRaises 显式断言，别让测试自己崩",
          "测试之间互不依赖、不共享可变状态，不依赖网络与真实文件"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def add(a, b):\n    return a + b\n\n\ndef test_add():\n    assert add(1, 2) == 3\n\n\nprint(test_add())\nprint(callable(test_add))",
        "expected": [
          "None",
          "True"
        ],
        "explain": "test_add 没有 return 所以打印 None；能被调用说明它就是个普通函数，pytest 会按名字收集 test_* 函数并执行。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def divide(a, b):\n    return a / b\n\n\ndef test_divide():\n    assert divide(1, 0) == 0",
        "rootCause": "把“应该抛异常”的路径写成了返回值断言：divide(1, 0) 抛的 ZeroDivisionError 会让测试直接报错，而不是验证预期行为。",
        "fix": "用 with pytest.raises(ZeroDivisionError): divide(1, 0)（或 assertRaises）明确断言异常类型。"
      },
      "code_task": {
        "prompt": "写 parse_int(text) 与覆盖正常、边界、异常三条路径的测试函数，不依赖网络或真实文件。",
        "acceptance": [
          "正常用例：parse_int(\" 42 \") == 42",
          "边界用例：覆盖带前后空白的输入",
          "异常路径：空串抛 ValueError，并用 pytest.raises 或 try/except 断言",
          "变体：测试之间不共享可变状态（重复运行结果一致）"
        ],
        "reference": "def parse_int(text):\n    return int(text.strip())\n\n\ndef test_parse_int_normal():\n    assert parse_int(\" 42 \") == 42\n\n\ndef test_parse_int_rejects_empty():\n    try:\n        parse_int(\"\")\n    except ValueError:\n        return\n    raise AssertionError(\"空串应该抛 ValueError\")"
      },
      "pitfalls": [
        "测试里只 print 不 assert",
        "用例之间共享可变状态",
        "只测正常路径",
        "测试依赖执行顺序或外部环境"
      ]
    },
    {
      "code": "py.quality.ruff-mypy",
      "title": "ruff/mypy 静态检查",
      "minutes": 15,
      "module": "工程化",
      "level": "基础",
      "taskRefs": [
        {
          "taskId": "1404",
          "relation": "introduces"
        }
      ],
      "concept": {
        "prompt": "ruff 和 mypy 各查什么？类型标注能替代运行期校验吗？",
        "answer": [
          "ruff 查风格、未使用变量、可疑写法（flake8 类规则）并能自动修复",
          "mypy 在运行前检查类型一致性，能发现传错类型、返回类型不符",
          "类型标注是给人、编辑器和 mypy 看的契约，运行时不会强制；外部输入仍需校验",
          "把检查写进 CI 与 pre-commit，规则在 pyproject.toml 里显式配置"
        ]
      },
      "predict": {
        "prompt": "写出下面代码的输出",
        "code": "def total(items: list[int]) -> int:\n    result = 0\n    for item in items:\n        result += item\n    return result\n\n\nprint(total([1, 2, 3]))\nprint(total.__annotations__[\"return\"].__name__)",
        "expected": [
          "6",
          "int"
        ],
        "explain": "标注不影响运行结果；__annotations__ 保存了参数与返回值类型，可用于自省。"
      },
      "debug": {
        "prompt": "找出下面代码的问题，说明根因并给出修法",
        "code": "def greet(name):\n    return \"hello \" + name\n\n\nprint(greet(123))",
        "rootCause": "没有类型标注，mypy 无法发现传入了 int；直到运行时字符串拼接才抛 TypeError，问题暴露得太晚。",
        "fix": "标注 def greet(name: str) -> str，把 mypy 纳入 CI；对来自外部（请求、JSON）的输入仍要运行期校验。"
      },
      "code_task": {
        "prompt": "给函数补全参数与返回值类型标注，并让 mypy 与 ruff 都能通过（配置写进 pyproject.toml）。",
        "acceptance": [
          "函数参数与返回值都有类型标注",
          "mypy 对该函数无 error",
          "ruff check 无 error",
          "配置中显式选择规则集/严格度，而不是全关"
        ],
        "reference": "def average(values: list[float]) -> float:\n    if not values:\n        raise ValueError(\"values 不能为空\")\n    return sum(values) / len(values)"
      },
      "pitfalls": [
        "以为类型标注会在运行时强制检查",
        "注解写成字符串引发前向引用问题",
        "忽略 ruff/mypy 报错",
        "把 lint 规则全关掉图省事"
      ]
    }
  ]
}
