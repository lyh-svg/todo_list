可以迁移。数据库是二进制文件，不能直接复制粘贴成普通文本；通过 Base64 编码即可把它安全地转换成文本，再在另一台电脑还原。
需要迁移两个正式数据库：
data/todo.sqlite3
data/memo.sqlite3
不要直接复制正在运行中的数据库。先关闭浏览器页面，等待本地服务退出。
在旧电脑项目目录执行：
cd /home/xiaoi/downloads/todo-list

python3 - <<'PY'
import base64
from pathlib import Path

data_dir = Path("data")
for name in ("todo.sqlite3", "memo.sqlite3"):
    source = data_dir / name
    encoded = data_dir / f"{name}.b64.txt"
    encoded.write_text(
        base64.b64encode(source.read_bytes()).decode("ascii"),
        encoding="ascii"
    )
    print(f"{encoded}: {encoded.stat().st_size} bytes")
PY
这样会生成：
data/todo.sqlite3.b64.txt
data/memo.sqlite3.b64.txt
用文本编辑器打开这两个 .b64.txt 文件，把全部内容复制到新电脑对应的文本文件中。注意不要复制文件名、空格或其他说明文字，只复制 Base64 内容。
在新电脑项目目录中，先创建 data 目录，然后执行：
cd /新电脑上的/todo-list
mkdir -p data
把复制来的文本保存为：
data/todo.sqlite3.b64.txt
data/memo.sqlite3.b64.txt
再执行还原：
python3 - <<'PY'
import base64
from pathlib import Path

data_dir = Path("data")
for name in ("todo.sqlite3", "memo.sqlite3"):
    encoded = data_dir / f"{name}.b64.txt"
    target = data_dir / name
    target.write_bytes(base64.b64decode(encoded.read_text(encoding="ascii")))
    target.chmod(0o600)
    print(f"已还原: {target} ({target.stat().st_size} bytes)")
PY
最后启动：
./open-ai-list.sh
验证数据库是否正常：
python3 - <<'PY'
import sqlite3

for path in ("data/todo.sqlite3", "data/memo.sqlite3"):
    con = sqlite3.connect(path)
    print(path, con.execute("PRAGMA quick_check").fetchone()[0])
    con.close()
PY
两个文件都显示：
ok
就说明迁移成功。
目前数据库大约只有几百 KB，Base64 后也不大。如果以后数据库变得很大，可以按每段几十万字符分段复制，但当前不需要。data/backups/Python.sqlite3 是备份历史，不影响程序运行，可以不迁移。